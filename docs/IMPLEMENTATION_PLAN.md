# Agents IDE — план реализации

Дата: 14 сентября 2026 года. Статус: этапы 0–2 выполнены в исходном объёме; расширение 2A и этапы 3–12 ожидают реализации.

[Журнал реализации](IMPLEMENTATION_LOG.md) хранит нюансы разработки, реализованные фичи, блокеры, результаты проверок и заметки для продолжения. Перед началом этапа проверять открытые вопросы журнала; после значимого изменения или рабочего прохода обновлять его вместе с отметками этого плана.

Основание: [описание проекта](PROJECT_DESCRIPTION.md). Редакция 3 добавляет группы моделей для агентов и обычных LLM с приоритетным выбором и переключением при недоступности; уточнения ревью из редакции 2 сохранены. Все исходные требования сохранены для полной v1, внутри неё выделен MVP. Контракты описаны в docs/architecture; проверки этапа 0 и каркас этапа 1 выполнены. Реальные результаты и границы допуска зафиксированы в [матрице интеграций](integrations/CAPABILITIES.md) и [решениях каркаса](architecture/FOUNDATION_DECISIONS.md).

## 1. Порядок работы и техническая основа

Сначала создаются серверный движок, fake/demo-исполнитель, управление и диагностический CLI. LLM, Command, CollectContext, Git и весь пресет проверяются независимо от реального harness. Затем подключаются первый harness и базовый UI: это внутренний MVP. В MVP входят API/UI групп моделей, приоритетный выбор и fallback для первого harness и LLM. Второй harness, переключение между harness и визуальный конструктор добавляются после его приёмки.

Номера этапов — идентификаторы работ, а не строгая последовательность. Рабочий маршрут: **0 (контракты) → 1–2 → 2A → 3–5 → 7 → 8 на fake → 6A → 9 → gate MVP → 6B → 10–11 → 12**. Проверки интеграций этапа 0 продолжаются независимо; отсутствие второго harness не блокирует этот маршрут.

Решения, зафиксированные при реализации этапов 0–1:

| Область              | Решение первой версии                                                                                |
| -------------------- | ---------------------------------------------------------------------------------------------------- |
| Frontend             | React, TypeScript, Vite; React Flow (`@xyflow/react`) для графа; TanStack Query для серверных данных |
| Backend              | FastAPI, Pydantic, отдельный Python worker с асинхронными адаптерами                                 |
| Хранение             | SQLite/WAL, SQLAlchemy, Alembic; отдельный каталог артефактов                                        |
| Очередь              | Устойчивая очередь и команды управления в БД; один worker-процесс с ограниченной конкурентностью     |
| Обновления UI        | Worker пишет БД; общий API poller 200 мс–2 с на Run; REST и cookie-auth SSE по курсору               |
| Codex                | App Server через stdio, протокол проверяется по установленной версии                                 |
| OpenCode             | Выделенный локальный server для рабочей области, HTTP + SSE                                          |
| Обычная LLM          | HTTP-адаптер OpenAI-compatible Chat Completions                                                      |
| Проверки разработки  | pytest, проверки типов Python, Ruff; TypeScript/ESLint, Vitest, Playwright для ключевых UI-сценариев |
| Сборка и зависимости | uv для Python, npm с lockfile для фронтенда; точные версии выбираются и фиксируются на старте        |
| Процессы Windows     | ProcessSupervisor, Job Objects, экземпляры harness на Run, скрытый фоновый launcher                  |
| Секреты и вход       | DPAPI user-scope, ACL; одноразовый pairing и same-origin cookie/CSRF                                 |
| Git                  | Default run_branch/strict; baseline, allowlist, временный index, intent и сверка hooks               |
| План и схема         | Фиксированные PlanItem, общий PlanControl; semver схемы и required_features                          |

Точные правила находятся в [STATE_MACHINES](architecture/STATE_MACHINES.md), [RUNTIME_CONTRACTS](architecture/RUNTIME_CONTRACTS.md), [EXECUTION_CONTRACTS](architecture/EXECUTION_CONTRACTS.md), [SECURITY_AND_OPERATIONS](architecture/SECURITY_AND_OPERATIONS.md). PostgreSQL, Redis, удалённые workers, worktree и installer не входят в v1.

Новое требование редакции 3: роль или узел выбирает конкретную модель либо группу типа `agent`/`llm`. Например, планирование использует `heavy`, обычное написание кода — `flash`; внутри каждой группы кандидаты вызываются по приоритету. Модель группы всегда привязана к конкретному harness-профилю или LLM-подключению. Семантика определена в [описании проекта, §7.5](PROJECT_DESCRIPTION.md#75-группы-моделей-и-приоритетное-переключение); архитектурные контракты пока содержат прежний запрет автоматической замены и должны быть синхронизированы в этапе 2A. Для прямого выбора этот запрет сохраняется.

## 2. Предлагаемая структура репозитория

```text
agents-ide/
  README.md
  backend/
    pyproject.toml
    uv.lock
    alembic/
    src/agents_ide/
      api/                 # REST, SSE, зависимости запросов
      domain/              # Сущности, схемы графа, состояния, события
      services/            # Проекты, чаты, шаблоны, настройки
      persistence/         # Модели БД, репозитории, транзакции
      engine/              # Переходы, условия, контекст, лимиты
      worker/              # Очередь, leases, управление, восстановление
      adapters/
        codex/
        opencode/
        llm/
        command/
        git/
      security/            # Секреты, пути, политики доступа
      presets/             # Версионированные определения и промпты
    tests/
      unit/
      integration/
      contracts/
      recovery/
  frontend/
    package.json
    package-lock.json
    src/
      app/
      features/
        projects/
        chats/
        pipelines/
        runs/
        settings/
      api/                 # Типы OpenAPI, запросы, SSE
      components/
    tests/
  docs/
    architecture/
      STATE_MACHINES.md
      RUNTIME_CONTRACTS.md
      EXECUTION_CONTRACTS.md
      SECURITY_AND_OPERATIONS.md
    integrations/
    operations/
    PROJECT_DESCRIPTION.md
    IMPLEMENTATION_PLAN.md
  scripts/                 # Разработка, сборка, фоновый запуск
```

БД, секреты и артефакты размещаются в настраиваемом каталоге данных приложения, вне управляемых репозиториев. Четыре архитектурных документа уже созданы; каталоги и код приложения создаются по мере выполнения этапов.

## 3. Этапы

### Этап 0. Проверить интеграции и зафиксировать контракты

Статус: проверка и решение go/no-go завершены. Реальные Codex/OpenCode, permission/auth/interrupt и восстановление завершённых сессий проверены. Восстановление незавершённых операций и изоляция разрешённой записи остаются `unverified`; автономная запись имеет `no-go` до закрытия gates 6A/6B. [Наблюдения и fixtures](integrations/CAPABILITIES.md).

Цель: проверить выбранные контракты минимальными прототипами и установить возможности harness. Документы задают решения, но не заменяют фактический probe.

- [x] Зафиксировать поддерживаемые ОС и версии Python/Node, проверить доступность harness и Git на машине worker.
- [x] Сверить официальные протоколы с установленными версиями и сохранить таблицу возможностей каждого адаптера.
- [x] На временном тестовом репозитории проверить для каждого harness запуск с конкретной моделью, получение событий, итог и ошибки.
- [x] Проверить получение каталога моделей, создание отдельной сессии, продолжение по явному ID и прерывание активного шага.
- [x] Проверить поведение при запросе разрешения и невозможности авторизации; определить отображение waiting_input.
- [x] Установить фактическую границу восстановления после обрыва транспорта и смерти процесса; непроверенные возможности пометить как неизвестные.
- [x] Сохранить обезличенные примеры событий и результатов в fixtures для контрактных тестов.
- [x] Проверить решения состояний/команд, SQLite/polling/SSE, процессов, путей, Git, секретов/доверия и поставки из архитектурных документов; расхождения оформлять изменением конкретного контракта.
- [x] Составить матрицу supported/unsupported/unverified с версией, датой и fixture; принять go/no-go транспорта и автономных ролей с записью.
- [x] Выбрать первый harness для 6A по реальному запуску, модели, ограничениям прав и управлению процессом; второй оставить для 6B.
- [x] Проверить прототип Windows Job Object с переживающим родителя потомком, directory identity и DPAPI под учётной записью фонового launcher.
- [x] Зафиксировать поддержанную среду и эталон нагрузки, негативные security-fixtures и условия допуска dirty_policy=allow_nonoverlap.

Результат: контрактная часть разрешает этап 1 независимо от готовности авторизации. Подтверждённый первый транспорт разрешает 6A, второй — 6B. Непроверенные возможности остаются unverified, fake не считается реальной интеграцией. Каталоги обоих harness остаются критерием полной v1.

Источники для проверки: [Codex App Server](https://learn.chatgpt.com/docs/app-server), [Codex non-interactive mode](https://learn.chatgpt.com/docs/non-interactive-mode), [OpenCode Server](https://opencode.ai/docs/server/), [OpenCode CLI](https://opencode.ai/docs/cli/).

### Этап 1. Создать каркас приложения

Статус: выполнен. [Установка и запуск](../README.md), [эксплуатация](operations/FOUNDATION.md).

Зависимость: технические решения этапа 0.

- [x] Создать backend и frontend с указанным стеком, зафиксировать зависимости и минимальные версии среды.
- [x] Настроить конфигурацию, структурированные логи, форматирование, статические проверки и сборку.
- [x] Добавить отдельные команды запуска API и worker; worker запускается независимо от Vite и браузера.
- [x] Ввести %LOCALAPPDATA%/AgentsIDE с db/artifacts/logs/secrets/runtime/temp, ACL, миграции, loopback/Host/Origin-проверки и настраиваемый API-порт.
- [x] Реализовать pairing, cookie HttpOnly/SameSite, CSRF, отзыв/истечение сессии и Vite proxy для same-origin REST/SSE.
- [x] Реализовать DPAPI SecretStore, secret_unavailable без удаления ciphertext и удаление секретов из журналов/argv.
- [x] Настроить единый формат ошибки API: код, понятное описание, детали, ID запроса.
- [x] Добавить health/readiness API и сохранённый heartbeat worker, различая доступность API и исполнителя.
- [x] Подготовить Windows CI для backend/Git/путей/процессов и сборки frontend; Linux дополняет, но не заменяет Windows.
- [x] Подготовить ранний фоновый launcher start/status/stop под текущим пользователем, без видимых окон и зависимости от консоли.

Критерий готовности: на чистой машине по README можно установить зависимости, запустить API и worker независимо, применить миграции и собрать пустой интерфейс.

### Этап 2. Реализовать данные и конфигурационные API

Статус: выполнен после ревью. Миграции `0002_domain` и `0003_stage2_review`, защищённые REST API, редактируемые черновики, неизменяемые версии и снимки Run, атомарная идемпотентность/команды, сервис резерваций и генерация контрактов проверены. Очередь пока не исполняется; полноценная валидация и worker относятся к этапам 3–5. Реальный HTTP-probe провайдера относится к этапу 7 и пока возвращает 501. [Реализованные контракты и обновление старых данных](architecture/DOMAIN_DATA.md), [результаты ревью](IMPLEMENTATION_LOG.md).

Зависимость: этап 1.

- [x] Создать модели Project, Chat, Message и операции создания, чтения, изменения и архивации.
- [x] Реализовать нормализацию путей, проверку доступности, Git-метаданные и выявление одного каталога через разные пути.
- [x] Создать PipelineTemplate, PipelineVersion и PipelineBinding; отделить черновик от неизменяемых версий.
- [x] Создать ProviderConnection, HarnessProfile и каталог моделей; вынести хранение ключей в SecretStore.
- [x] Определить порядок переопределения параметров и сохраняемый итоговый снимок Run.
- [x] Подготовить Run, StepExecution/StepAttempt, AgentSession, RunEvent, ArtifactManifest, QueueJob/lease/generation, CommandJournal, WorkspaceReservation и PlanItem. Capability, причины и интервалы — типизированные поля; отдельные таблицы только при необходимости.
- [x] Добавить старт по idempotency_key, команды по command_id/hash/expected_state_version, policy revisions лимитов и immutable input/snapshot.
- [x] Хранить directory identity и запрет обхода резерваций через алиасы/вложенные каталоги/общий checkout; UNC/сетевые/WSL пути отклонять.
- [x] Добавить уникальные ограничения, связи, индексы и оптимистическую проверку версии при редактировании черновиков.
- [x] Определить правила архивации/удаления: активный Run и используемые им настройки нельзя разрушить; история сохраняется.
- [x] Генерировать TypeScript-типы API и node schemas из серверного контракта; CI проверяет синхронность (`scripts/generate_contracts.py --check`). Полная схема графа и AST развиваются на этапе 3.

Критерий готовности: через API создаются проекты, чаты, подключения и версии pipeline; после перезапуска данные совпадают. Ключ можно записать/заменить, но нельзя прочитать обратно через API. Снимок Run не меняется при редактировании источника.

### Этап 2A. Добавить группы моделей и конфигурацию выбора

Статус: новое расширение, не реализовано; не входит в выполненные отметки исходного этапа 2.

Зависимость: этап 2. Контракты и данные готовятся до этапа 3; исполнение добавляется в этапах 4–7, UI — в этапах 9–11.

- [ ] Синхронизировать STATE_MACHINES (immutable snapshot, resume и переход к следующему кандидату), RUNTIME_CONTRACTS (каталог событий/причин, capability, retry/fallback и бюджеты), EXECUTION_CONTRACTS (выбор исполнителя, preflight и сессии) и SECURITY_AND_OPERATIONS (все назначения данных группы и доверие execution_hash) с §7.5 описания проекта. Снять запрет автоматической замены только для кандидатов группы из snapshot.
- [ ] Добавить миграцию, ORM/Pydantic-схемы ModelGroup и ModelGroupMember: стабильные ID, имя/описание, тип agent/llm, ревизия, порядок, включение, точные model IDs, профиль/подключение и параметры кандидата. Имя уникально внутри типа; вложенные/смешанные группы и дубликаты пары профиль/подключение + модель запрещены.
- [ ] Реализовать API создания, чтения, изменения, копирования и архивации групп; атомарное изменение порядка и оптимистическую проверку ревизии. Использование группы требует хотя бы одного включённого кандидата; архивная группа недоступна для новых привязок/Run, её история сохраняется.
- [ ] Добавить типизированный выбор `direct` или `group` для ролей, узлов, проектных привязок и переопределений запуска; группа agent допустима только для AgentTask, llm — для LLMRequest. Одиночный агентный запрос использует тот же выбор.
- [ ] Сохранить приоритет настроек запуск → binding → версия pipeline → глобальные значения; выбор direct/group переопределять целиком. Параметры разрешать отдельно для каждого кандидата: узел → роль → кандидат → профиль/подключение. Списки из разных уровней не объединять.
- [ ] Фиксировать в Run ID/ревизию группы, порядок, включение, идентификаторы и несекретные настройки профилей/подключений (включая назначения данных) и разрешённые параметры кандидатов; учитывать разрешённую конфигурацию в execution_hash. Секреты остаются в SecretStore; редактирование/архивация источников не меняет snapshot и не разрушает данные, нужные активному Run.
- [ ] Сохранить совместимость существующих конфигураций и Run с прямой моделью без fallback; обновить серверный контракт, required_features и генерируемые TypeScript-типы. Импорт/экспорт переносит определения групп без секретов, требует привязки внешних профилей/подключений и не перезаписывает одноимённые локальные группы.

Критерий готовности: через API создаются agent/llm-группы `heavy` и `flash`, порядок и параметры сохраняются после перезапуска; конфликт ревизий отклоняется. Роли выбирают direct или group, snapshot сохраняет полный список независимо от последующего редактирования группы. Новые требования отражены в архитектурных контрактах; старые конфигурации работают без автоматической подмены модели.

### Этап 3. Создать описание графа и серверную валидацию

Зависимости: этапы 2 и 2A.

- [ ] Определить JSON-схему: semver schema_version, required_features, входы/роли, execution_hash и лимиты 200 узлов/400 связей/1 MiB.
- [ ] Описать Start, AgentTask, LLMRequest, CollectContext, Command, Condition, GitCommit, PlanControl, End и выдачу схем клиенту.
- [ ] Описать Command со списком команд и фильтром ID, required/success_exit_codes/failure_policy/cwd/env/timeout/output cap/retry_safety.
- [ ] Описать CollectContext collect/resolve_requests, sources из context_paths, manifests, truncation/omissions и ограничения путей.
- [ ] Описать PlanControl select_next/record_verified/record_final_check/attach_commit как обычный тип узла.
- [ ] Разделить параметры исполнения и координаты/визуальное оформление узлов.
- [ ] Реализовать AST с трёхзначной логикой, строгими типами, отсутствующими значениями и ошибками ссылок; latest.decision привязан к области/циклу.
- [ ] Реализовать ссылки на контекст, безопасную подстановку переменных и проверку обязательных входов.
- [ ] Определить присваивания на переходах, выбор initial/repair/next_item и правила актуальности данных между циклами.
- [ ] Добавить валидацию связности, единственного Start, достижимости End, выхода из циклов и лимитов обратных переходов.
- [ ] Разрешить сохранение неполного черновика; запретить его запуск до успешной валидации.
- [ ] Реализовать импорт/экспорт с проверкой schema/features/лимитов и доверия конкретному hash; поле trusted из импорта игнорируется.
- [ ] Реализовать preflight с источниками настроек, правами, назначениями данных, Git-планом и повторной проверкой перед dispatch; неподдержанная версия не мигрируется молча.
- [ ] Проверять direct/group, тип группы и ссылки каждого кандидата; показывать порядок, итоговые параметры, capability/контекст/формат ответа/бюджет, все назначения данных и причины недоступности/несовместимости. Ошибочные параметры блокируют конфигурацию; недоступный первый кандидат не блокирует запуск при наличии следующего подходящего. Не отправлять платные probe-запросы автоматически.

Критерий готовности: валидный граф с двумя обратными связями принимается сервером. Несуществующая переменная, неоднозначный переход, цикл без лимита и неподдержанный параметр модели дают ошибку с ID узла/связи.

### Этап 4. Реализовать движок на тестовом исполнителе

Зависимость: этап 3.

- [ ] Создать fake/demo-режим: события/ответы/задержки/сбои, сценарные правки только в тестовом каталоге, simulated-маркировка и отсутствие сети.
- [ ] Реализовать атомарный старт: снимок Run, запись очереди и событие в одной транзакции; повтор HTTP-запроса с тем же ключом не создаёт дубликат.
- [ ] Реализовать захват короткой BEGIN IMMEDIATE, busy_timeout, lease/generation, лимит 2 Run и резервации; внешние вызовы вне транзакций.
- [ ] Сохранять StepExecution для каждого посещения и StepAttempt перед внешним вызовом.
- [ ] Реализовать общий серверный выбор кандидата для agent/llm по snapshot: пропуск отключённых/несовместимых, первый доступный, fallback только вперёд и без зацикливания. Retry остаётся на том же кандидате; новый StepExecution, включая repair, начинает с первого приоритета, resume прерванного посещения сохраняет его кандидата по §7.5 описания проекта.
- [ ] Различить подтверждённую недоступность (сразу следующий кандидат) и временный сбой (сначала ограниченный retry по политике узла). Fallback разрешать только до отправки либо при доказанном отказе без полезного исполнения; unknown/частичные эффекты направлять на восстановление, неверный JSON/вердикт/ошибку конфигурации/permission — в собственные политики.
- [ ] Каждому вызову кандидата выделять StepAttempt в том же посещении; сохранять группу/ревизию, member ID/позицию, фактическую модель/подключение, причину переключения и результат. Вызовы retry/fallback расходуют общие лимиты Run, пропуск без внешнего вызова — только событие. Идемпотентность у одного провайдера не разрешает повтор у другого.
- [ ] Добавить события выбора/пропуска/переключения/исчерпания группы; после исчерпания — waiting_input(model_group_exhausted) с диагностикой всех кандидатов. Новый проход сохранённого списка возможен после восстановления доступа и явного продолжения, без сброса бюджетов; limit_exceeded запрещает новые вызовы.
- [ ] Реализовать последовательное выполнение, выбор перехода, присваивание контекста и фиксацию результата.
- [ ] Разделить бизнес-вердикт проверки, техническую ошибку вызова и терминальную ошибку процесса.
- [ ] Нормализовать проверочный вердикт в `decision`: true/false/null для passed/failed/inconclusive; сохранить исходный ответ и проверить маршрут unknown отдельно от false.
- [ ] Сохранять счётчики/интервалы времени, retry_at, лимит внешних вызовов и optional tokens/cost с observed/estimated/unknown; resume не сбрасывает использование.
- [ ] Добавить проверку структурированных ответов и отдельный сценарий невалидного JSON/inconclusive.
- [ ] Сохранять нормализованные события и артефакты, включая входы, фактический промпт и причину перехода.
- [ ] Реализовать каталоги событий/ошибок, manifests, payload cap, транзакционный sequence и grouping только текстовых delta до 100 мс.
- [ ] Реализовать snapshot/cursor, общий API poller, cookie-auth SSE, backpressure, replay/reset_required и дедупликацию до полноценного UI.

Критерий готовности: полностью без фронтенда через API запускается граф «реализация → failed → исправление → passed → End». Проверяется смена промпта, история двух посещений и остановка на лимите.

### Этап 5. Добавить управление и восстановление

Зависимость: этап 4. Этот этап выполняется до допуска реальных агентов к автономным правкам.

- [ ] Реализовать таблицы Run/StepExecution/StepAttempt, unknown/reconciliation и команды с command_id/expected_state_version.
- [ ] Применить приоритет cancel > stop > pause, сохранение blockers, отсутствие неявного resume и аудит лимитов.
- [ ] Для паузы сохранять запрос и завершать текущий шаг; следующий шаг не стартует.
- [ ] Для stop дождаться подтверждения остановки внешнего вызова; поздний ответ пометить, но не применять к продвижению графа.
- [ ] Для resume различать запуск следующего шага и новую попытку прерванного; сохранить счётчики и старые результаты.
- [ ] Добавить lease, heartbeat и поколение владельца; протестировать конкуренцию двух экземпляров worker, даже если штатно используется один.
- [ ] Добавить восстановление recovering: сверка внешнего статуса, журналов и рабочего дерева перед продолжением.
- [ ] Сохранять позицию и историю выбора, retry_at и лимиты группы; restart/resume не начинает список заново и не запускает второго кандидата при неизвестном исходе первого. Stop/cancel/pause проверяются до каждого retry/fallback; после восстановления доступа из model_group_exhausted допускается только явный новый проход списка из snapshot.
- [ ] Сохранять резервацию каталога во время паузы/остановки; снимать её после подтверждённого завершения или отмены.
- [ ] Реализовать ProcessSupervisor и Job Objects с kill-on-job-close, без breakaway, включением процесса до исполнения и проверкой потомков.
- [ ] Реализовать interrupt/kill deadlines, реестр PID/start time/generation, health/порты и stale cleanup только своих процессов.
- [ ] Разделить worker heartbeat, attempt heartbeat, process health и последнее внешнее событие; тишина вывода не означает зависание.
- [ ] Обработать гонки stop/completion, cancel/retry, потерю lease и повторную доставку события.
- [ ] Обеспечить безопасный выход при недоступной БД: запрет новых действий и остановка управляемой работы, результат которой нельзя надёжно учесть.
- [ ] Добавить ранний CLI через API: проекты/чаты, start/status/events/artifacts, pause/stop/resume/cancel/resolve и диагностика waiting_input.
- [ ] Добавить обслуживание stale reservation только после доказанной остановки процессов, без обхода живого владельца.

Критерий готовности: принудительное завершение worker в разных фазах не запускает второй живой исполнитель в том же каталоге. Неизвестный исход приводит к waiting_input; API restart не останавливает живой worker. После остановки можно продолжить с сохранённым контекстом.

### Этап 6. Реальные harness: 6A первый, 6B второй

Зависимости: probe конкретного harness из этапа 0 и этап 5. Подэтап 6A подключает первый выбранный harness, 6B начинается после gate MVP. Общие задачи ниже выполняются для каждого отдельно, без блокировки первого вторым.

- [ ] Реализовать общий интерфейс адаптера и матрицу возможностей, без ветвления по harness внутри движка.
- [ ] Для Codex реализовать инициализацию транспорта, управление thread/turn, запрос моделей, обработку сообщений/инструментов, итогов и interrupt.
- [ ] Для OpenCode реализовать server на Run, loopback-порт, credentials/health/directory/session, каталог, HTTP/SSE и abort; не подключаться к чужому процессу на занятом порту.
- [ ] Сохранять внешние session/turn/message IDs до дальнейших зависимых действий, насколько позволяет протокол; неоднозначный старт передавать механизму восстановления.
- [ ] Добавить политики сессий: новая для роли/запуска, продолжение конкретной сессии, новая с передачей сохранённого контекста при невозможности resume.
- [ ] Ограничить repair-продолжения до 3, затем создавать новую сессию с резюме/evidence; очередной PlanItem по умолчанию начинает новую сессию.
- [ ] Подключить sandbox/permissions harness и обработку ожидающих запросов пользователя.
- [ ] Нормализовать события агентов и метрики, сохранять доступные исходные события после удаления секретов.
- [ ] Учесть обрыв SSE/stdio, завершение процесса, несовпадение версии протокола, недоступную модель и отказ авторизации.
- [ ] Добавить кэш каталога с датой, ручное обновление и диагностику; при direct не подменять модель, при group выбирать только кандидатов сохранённого списка через общий механизм движка.
- [ ] Для групп нормализовать недоступность модели/профиля, отказ авторизации, квоты и transport errors с retry_safety/external_outcome. При смене кандидата создавать новую AgentSession с ограниченным контекстом/evidence, проверенными правами и без переноса native session ID; сначала подтверждать отсутствие живой предыдущей операции.
- [ ] Применить TTL=15 минут, инвалидацию при смене версии/credential/подключения, manual ID с unverified при деградации.
- [ ] Проверить на обоих harness длинный шаг, stop/resume, отдельную проверочную сессию и работу без открытого браузера.

Критерий 6A: первый harness реально выполняет пункт, stop/resume и проверочную роль в тестовом проекте с выбранной моделью и подтверждёнными правами; группа переключается после подтверждённого отказа первого кандидата. Критерий 6B: второй проходит ту же приёмку; оба автоматических каталога моделей подтверждены, группа переключается между Codex/OpenCode с новой сессией и сохранением контекста/прав. Ограничения фиксируются по версии; fake-fixtures не заменяют эти проверки.

### Этап 7. Подключить LLM, команды и сбор доказательств

Зависимости: этапы 3–5; выполняется независимо от этапа 6, до реального harness достаточно fake-сценария.

- [ ] Реализовать LLM-подключения с URL, секретом, ручными моделями и получением каталога, если доступно.
- [ ] Добавить проверку формата настроек и явный тестовый запрос подключения; каталог моделей сам по себе не считается проверкой доступа.
- [ ] Реализовать HTTP-вызов, поток при поддержке, timeout/10 MiB response cap, проверку URL, loopback HTTP/remote HTTPS, запрет ключей в URL и переноса credentials через redirect.
- [ ] Проверять возможности structured output; при его отсутствии запросить JSON и валидировать результат на сервере.
- [ ] Реализовать список Command, required/success_exit_codes, collect_all/stop_on_failure, минимальное env, subcommand ledger, вывод/exit code/timeout и остановку дерева.
- [ ] Реализовать CollectContext: выбранные файлы, diff, артефакты, результаты команд, исходный HEAD и manifest/hash текущего содержимого.
- [ ] Отмечать обрезанный/отсутствующий контекст; ограничить размер, исключить секреты и выход за рабочие пути через ссылки.
- [ ] Реализовать manifests, типизированные missing_evidence, отказ новым argv/правам от модели и повтор только настроенных safe command IDs.
- [ ] Сохранять доказательства по циклу и области проверки; не использовать успешный отчёт от старой версии файлов.
- [ ] Required-команда с кодом вне success_exit_codes, skipped или timeout исключает passed; исходный ответ модели сохраняется отдельно.
- [ ] Реализовать retry_safety для 429/5xx/connect/timeout, Retry-After, backoff/jitter; неизвестный исход платного запроса не повторять автоматически.
- [ ] Подключить LLM-группы к общему выбору кандидата, включая разные ProviderConnection и локальный сервер: параметры и ключ каждого подключения изолированы, логический запрос/evidence сохраняются. Проверять контекст и формат для каждой модели; fallback не обходит лимиты и не маскирует неизвестный исход.

Критерий готовности: обычная LLM проверяет переданный пакет после агентного шага, а обязательный упавший тест, невалидный JSON и недостаток данных дают предусмотренный неуспешный/неопределённый результат. Проверка работает с провайдером без endpoint каталога моделей. В группе недоступность первой модели приводит к следующей, в том числе через другое подключение; unknown после отправки блокирует автоматический fallback.

### Этап 8. Реализовать GitCommit и встроенный пресет

Зависимости: этапы 3–5 и 7. Пресет и Git сначала проверяются на fake + LLM/тестовом HTTP + Command. Реальный harness не нужен для серверного gate; после 6A те же сценарии проходят с ним.

- [ ] Реализовать branch_policy run_branch/current, dirty_policy strict/allow_nonoverlap, чистый index, baseline/status/manifests/ref и защиту пользовательских файлов.
- [ ] Реализовать GitCommit через временный index и проверенный allowlist, message/trailer intent ID, SHA/actual tree/no_changes, не меняя пользовательский index.
- [ ] Сохранять намерение коммита до выполнения; восстанавливать результат по ожидаемому дереву и фактической истории Git.
- [ ] Проверять внешние изменения и изменения от hooks; при расхождении с проверенными файлами возвращать к проверке либо ждать вмешательства.
- [ ] Проверить interactive signing, смену hooks/config и созданный коммит с неожиданным tree; не выполнять автоматические reset/amend/stash.
- [ ] Описать встроенный пресет обычным JSON-графом с версиями, входами, схемами результата и промптами из описания проекта.
- [ ] Настроить цикл исправления реализации и отдельный цикл ревью с повторными проверками после правок.
- [ ] Настроить проверку всего плана после коммита: финальный контекст включает исходный план, код и историю всех изменений запуска.
- [ ] Реализовать фиксированные PlanItem, PlanControl, item_results, серверный выбор ID, связь evidence/commit и проверку всех ID; текст без разбивки становится одним P1.
- [ ] Добавить контур evidence с лимитом 2 и no_progress; ручное разрешение ожидания не заменяет проверку кнопкой passed.
- [ ] При возврате по оставшимся пунктам переключать initial/repair/next_item корректно; сбрасывать только лимиты новой части, сохраняя общие лимиты Run.
- [ ] Добавить идемпотентную установку пресета: обновление приложения не перезаписывает пользовательские копии.
- [ ] Добавить проверку готовности привязок: запуск запрещён, пока обязательные роли не получили конкретные модели либо подходящие группы. Проверить пример implementer → agent/flash, reviewer и plan_checker → agent/heavy, verifier → llm/heavy; имена и реальные модели не зашивать в пресет.

Критерий серверного gate: два фиксированных пункта, failed→repair, правка после ревью, inconclusive→evidence и завершение проходят через API на тестовом каталоге. Коммиты ожидаемы, crash после commit не создаёт дубликат, no_changes не мешает оценке оставшегося плана. Перед MVP сценарий повторяется с первым реальным harness и реальной LLM-проверкой.

### Этап 9. Базовый интерфейс и gate внутреннего MVP

Зависимости: этапы 2–5, 7–8 и 6A для реальной приёмки. UI можно начать на fake; он использует готовые REST/SSE, не ожидая визуального конструктора.

- [ ] Создать layout: проекты слева, чаты выбранного проекта, основная область и отдельные настройки.
- [ ] Реализовать добавление/редактирование проекта и понятные ошибки пути; формы создания и архивации чатов.
- [ ] Показывать сообщения, выбор одиночного агента или pipeline, параметры запуска и список Run.
- [ ] Реализовать настройки harness, диагностику, выбор модели и её поддержанных параметров.
- [ ] Реализовать LLM-подключения: URL, маскированный ключ, замена ключа, модели и тест запроса.
- [ ] Реализовать экран групп agent/llm: создание, переименование, копирование, архивация, добавление/удаление/отключение кандидатов, изменение порядка и параметров. В выборе роли/одиночного агента различать direct и group, показывать тип и все позиции.
- [ ] Реализовать библиотеку шаблонов, версии, копирование пресета и проектные привязки.
- [ ] Показывать происхождение итоговых настроек и недостающие входы перед стартом.
- [ ] Обработать загрузку, пустое состояние, ошибки API, недоступный worker и потерю соединения.
- [ ] Добавить базовый экран Run: PlanItem progress, события/артефакты, process health, причины ожидания и команды с command_id и accepted/applied.
- [ ] Показывать выбранную группу, фактическую модель/профиль/подключение, приоритет и причины переключений; при model_group_exhausted — диагностику каждой позиции и продолжение после восстановления доступа. Предупреждать, что изменения группы применятся только к новым Run.
- [ ] Реализовать cookie/SSE reconnect/reset_required, истечение сессии, старый cursor и отображение качества бюджетных метрик.
- [ ] Показать необязательную подсказку об одной модели у implementer/verifier; не превращать её в блокировку.
- [ ] Пройти gate MVP: циклы/Git/stop/resume/recovery/отсутствие браузера с одним реальным harness, LLM и пресетом.
- [ ] Включить в gate группы heavy/flash: настройку в UI, разные привязки ролей, fallback первого harness и LLM, исчерпание списка, сохранение snapshot/порядка/бюджетов после сбоя; межharness-сценарий завершить в 6B.

Критерий gate MVP: из UI создаются проект/чат/подключения, запускается пресет с первым harness и LLM; stop/resume и восстановление согласованы с сервером. Все вкладки закрываются во время нескольких переходов, после открытия история восстановлена. Блокирующие проверки состояний, процессов, путей, секретов и Git пройдены. Только затем начинаются 6B и полный конструктор; MVP не объявляется полной v1.

### Этап 10. Создать визуальный конструктор

Зависимости: этап 3 и gate MVP этапа 9. Проверка обеих интеграций требует 6B, но разработка форм от него не зависит.

- [ ] Добавить холст React Flow, палитру, создание/удаление/дублирование узлов и редактирование связей.
- [ ] Создать формы из серверных JSON Schema, включая PlanControl, список Command и sources CollectContext; оставить специальные редакторы промптов/условий.
- [ ] Добавить direct/group в AgentTask/LLMRequest, фильтр типа группы, обзор приоритетного списка и источник переопределения роли/узла. Импорт групп требует разрешения ссылок на локальные профили/подключения и проверки всех назначений данных.
- [ ] Добавить редактор условий, выходы true/false/unknown, настройки обратных переходов и лимитов.
- [ ] Добавить подсказки переменных контекста, выбор источников данных и начальных значений.
- [ ] Реализовать сохранение черновика, отдельное сохранение версии и проверку конфликта редактирования.
- [ ] Отображать серверную валидацию на конкретном узле или связи; клиентская проверка служит удобству и не заменяет серверную.
- [ ] Дать возможность открыть готовый пресет, изменить проверяющего с агента на LLM и сохранить пользовательскую копию.
- [ ] Реализовать импорт/экспорт, восстановление раскладки и предупреждение о несохранённых правках.
- [ ] Показать сводку всех программ/argv/ролей/путей/назначений при импорте, доверие execution_hash и его сброс при исполняемой правке.
- [ ] Запретить параллельные выходы в редакторе; показывать schema_version/features/capability до запуска.

Критерий готовности: пользователь собирает цикл «сделай → проверь → исправь» через UI без ручного JSON, сохраняет версию и запускает её. Открытый пресет соответствует исполняемому графу, а координаты узлов не влияют на поведение.

### Этап 11. Реализовать наблюдение и управление в UI

Зависимости: этапы 5, 9 и 10.

- [ ] Расширить экран этапа 9 графом/timeline, фильтрами и историей, используя snapshot/SSE этапа 4.
- [ ] Проверить длительный reconnect, reset_required после retention, дедупликацию и медленного клиента с ограниченным буфером.
- [ ] Отобразить активный узел, выбранную связь, цикл, попытки, время, агента, модель и результат проверки.
- [ ] Отобразить группу/ревизию, попытки по кандидатам, пропуски и переходы между моделями/подключениями; после reconnect/restart восстановить ту же историю из серверных событий и snapshot.
- [ ] Добавить timeline, сообщения и доступные вызовы инструментов, просмотр отчётов, diff, команд и артефактов.
- [ ] Добавить кнопки «Пауза», «Остановить», «Продолжить», «Отменить» и различимые промежуточные состояния.
- [ ] Реализовать waiting_input: конкретная причина, недостающие данные/разрешение и продолжение после решения.
- [ ] Ограничить объём отрисовываемых логов, использовать порционную загрузку и виртуализацию длинной истории.
- [ ] Различать отсутствие связи с API и остановку самого Run; не вычислять серверные переходы на клиенте.

Критерий готовности: после закрытия всех вкладок на несколько шагов работа продолжается; повторное открытие восстанавливает актуальную позицию и события без потерь/дубликатов. Несколько вкладок наблюдают один и тот же Run, не создавая повторных действий.

### Этап 12. Проверить полный продукт и подготовить эксплуатацию

Зависимость: этапы 0–11.

- [ ] Пройти матрицу приёмки ниже на fake-адаптере и критические сценарии на реальных Codex/OpenCode/LLM.
- [ ] Проверить миграцию уже заполненной БД и сохранение пользовательских шаблонов при обновлении пресета.
- [ ] Проверить неизвестный schema major/features и update при нетерминальном Run; snapshot не переписывается, миграция создаёт новую версию.
- [ ] Проверить фоновые запуск/перезапуск API и worker на Windows, работу без Vite в собранном приложении.
- [ ] Подготовить команды сборки frontend и отдачу статических файлов через FastAPI; описать режим разработки отдельно.
- [ ] Описать установку/авторизацию harness, создание проекта, прямой выбор моделей и группы heavy/flash, приоритеты, привязки ролей, диагностику fallback/model_group_exhausted и применение пресета.
- [ ] Реализовать квоты/retention/GC по manifests, защиту active/pinned Run и критичных evidence, disk full и компактацию на безопасной границе.
- [ ] Реализовать согласованный backup SQLite/артефактов, hashes, recovery после restore и secret_unavailable без DPAPI; рабочие репозитории автоматически не копируются.
- [ ] Прогнать 100 000 событий, большие выводы/артефакты, slow SSE и пагинацию; измерить p95 из эксплуатационного контракта.
- [ ] Проверить injection в README/evidence, импорт с shell, .env, redirects, pairing/CSRF/Host и подмену ссылок.
- [ ] Описать stop/resume, восстановление неизвестного результата, поведение при выключении компьютера и ограничения поддержанных версий.
- [ ] Добавить диагностику недоступного worker, диска/БД, авторизации, моделей и команд проверок.
- [ ] Довести launcher/diagnostics/backup/restore/update; автозапуск Task Scheduler настраивается отдельно пользователем под той же учётной записью.
- [ ] Проверить поставку с готовым UI, закрытие консоли, reboot/recovery, занятый порт и инструкции на чистой Windows-машине.
- [ ] Проверить, что ключи не появляются в OpenAPI-ответах, логах, событиях, URL и экспорте.

Критерий готовности: пользователь следует README на чистой поддержанной среде и выполняет весь исходный сценарий. Все обязательные пункты матрицы приёмки пройдены; неподтверждённые реальные интеграции не выдаются за готовые.

## 4. Матрица приёмки

| ID  | Сценарий                                                              | Ожидаемый результат                                                                                                 |
| --- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| A01 | Два проекта, несколько чатов, перезапуск                              | Структура и сообщения восстановлены, пути корректны                                                                 |
| A02 | Один шаблон в двух проектах                                           | Разные bindings без изменения общей версии                                                                          |
| A03 | Изменение шаблона во время Run                                        | Run продолжает сохранённую версию; новый запуск использует выбранную новую                                          |
| A04 | Шаги Codex и OpenCode                                                 | Выбраны реальные модели, сохранены session IDs, события и результат                                                 |
| A05 | LLM с URL/ключом и без каталога моделей                               | Ручной ID работает, ключ не читается обратно, проверка получает evidence                                            |
| A06 | Проверка возвращает failed, затем passed                              | Возврат к нужному узлу с repair-промптом и замечаниями, затем движение вперёд                                       |
| A07 | Ревью вносит изменения                                                | Проверки повторяются по новому содержимому до коммита                                                               |
| A08 | После коммита план ещё не завершён                                    | Возврат с next_item и remaining_items; все пункты приводят к End                                                    |
| A09 | Невалидный JSON, inconclusive, провал обязательного теста             | Нет ложного успеха и неверного перехода по false при технической ошибке                                             |
| A10 | Исчерпан лимит цикла, времени или посещений                           | waiting_input с причиной; resume не обнуляет счётчики                                                               |
| A11 | Pause и resume                                                        | Текущий шаг заканчивается, следующий ждёт, продолжение идёт с границы                                               |
| A12 | Stop во время агента/команды и resume                                 | Остановка подтверждена, дочерние процессы учтены, создаётся новая попытка                                           |
| A13 | Cancel, повтор кнопки, поздний ответ                                  | Run не возобновляется; внешняя операция остановлена или её исход отмечен; граф не продвигается                      |
| A14 | Все браузеры закрыты                                                  | Worker продолжает до End либо явного состояния ожидания/ошибки                                                      |
| A15 | Обрыв SSE и открытие нескольких вкладок                               | Состояние одинаково, события восполнены, запуск не дублируется                                                      |
| A16 | Перезапуск API при живом worker                                       | Выполнение не прерывается                                                                                           |
| A17 | Сбой worker до/после внешнего вызова                                  | Reconciliation без двух исполнителей; неизвестный исход не повторяется автоматически                                |
| A18 | Два Run для одного каталога через разные пути                         | Второй ожидает резервацию, включая паузу первого                                                                    |
| A19 | Сбой после Git commit до фиксации результата в БД                     | Уже созданный коммит найден, повторный не создаётся                                                                 |
| A20 | Исходные или внешние изменения пользователя                           | Пресет не коммитит их; сообщает причину блокировки или требует повторной проверки                                   |
| A21 | Запрос разрешения harness / потеря авторизации                        | Причина видна и сохранена, нет скрытого бесконечного ожидания                                                       |
| A22 | Попытка выхода путей за проект / секрет в экспорте                    | Пути отклонены, секреты исключены из доступных пользователю экспортов и журналов                                    |
| A23 | Обновление и восстановление backup                                    | Версии/история/артефакты согласованы; недоступные DPAPI-секреты требуют восстановления доступа или повторного ввода |
| A24 | Stop/completion и cancel/End в обоих порядках                         | Применяется зафиксированный порядок, успешный эффект не повторяется, новый шаг не стартует после принятого stop     |
| A25 | Две вкладки посылают stop/resume, повтор command_id                   | Дубликат возвращает прежний результат; другой payload/старая версия даёт 409                                        |
| A26 | Повтор Start с тем же idempotency_key                                 | Один Run и один исходный QueueJob, различающийся payload отклонён                                                   |
| A27 | Worker пишет события без прямого канала к API                         | Poller доставляет сохранённое, restart API восстанавливает cursor, транзакция не удерживается внешним вызовом       |
| A28 | SSE cursor старше retention, cookie истекла                           | Новый снимок с границей истории либо повторный вход; Run продолжает работу                                          |
| A29 | Живой worker, зависший harness/долгая pause_requested                 | Раздельная диагностика, timeout и подтверждённая остановка либо process_not_responding                              |
| A30 | Потомок переживает родителя; OpenCode-порт занят                      | Job/supervisor обнаруживает своё дерево, чужой PID/порт не завершается; есть ограниченный retry/ошибка              |
| A31 | Регистр, slash, 8.3, subst, junction и вложенные проекты              | Идентичность/пересечение не позволяет два одновременно пишущих Run                                                  |
| A32 | UNC, сетевой диск, подмена symlink, длинный путь                      | Неподдержанное отклонено; выход за workspace закрыт при открытии, ограничения длинных путей показаны                |
| A33 | Список Command с required/optional и нестандартным success_exit_codes | Результат каждой команды сохранён, policy соблюдена, skipped required исключает passed                              |
| A34 | Огромный stdout, env и попытка shell-инъекции через аргумент          | Вывод ограничен/помечен, argv не конкатенируется, credentials не наследуются                                        |
| A35 | Strict/allow_nonoverlap, untracked/ignored и исходный dirty index     | Разрешена только заданная политика, пользовательские файлы защищены, index не включён в коммит                      |
| A36 | Hook изменил tree; signing требует ввода                              | Расхождение обнаружено, созданный коммит не принят как проверенный, нет скрытого зависания/reset                    |
| A37 | Run branch/current, no_changes при оставшихся пунктах                 | Ветка выбрана заранее, baseline ref существует, no_changes идёт к проверке всего плана                              |
| A38 | Исчерпаны вызовы/tokens/estimated cost                                | Новые вызовы блокированы, ожидание с причиной; policy revision не сбрасывает использование                          |
| A39 | Телеметрия бюджета отсутствует                                        | Показано unknown; строгий неподдержанный бюджет отклоняется, не считается нулём                                     |
| A40 | 100 000 событий, медленный SSE, большие артефакты                     | Буферы ограничены, пагинация работает, измерены p95, квота не уничтожает активные evidence                          |
| A41 | Импорт с Command/скрытым узлом/trusted=true                           | Сводка полная, запуск до доверия hash запрещён, trusted из файла игнорируется                                       |
| A42 | Injection в README/PAYLOAD/diff просит .env/commit/смену критериев    | Граф/критерии/разрешения не меняются, доступ ограничен, скрытого действия нет                                       |
| A43 | Чужой Origin/Host, неверный CSRF, повтор pairing                      | Нет доступа; код одноразовый, cookie/код/CSRF не попали в URL и логи                                                |
| A44 | Ключ в URL, HTTP remote, redirect на другой origin                    | Настройки/редирект отклонены, Authorization не переслан; loopback HTTP поддержан                                    |
| A45 | Restore без DPAPI и секрет в диагностическом артефакте                | История сохранена, secret_unavailable явен, подозрительный контент редактируется/карантинируется                    |
| A46 | Нет resume/interrupt/permissions у адаптера                           | Только объявленная деградация; автономная запись без ограничений запрещена                                          |
| A47 | Каталог устарел/модель исчезла/авторизация отказала                   | TTL/инвалидация работают, direct не подменяется; group следует snapshot и правилам fallback; каталог отличим от доступа |
| A48 | Много PlanItem; агент выбрал неизвестный/уже done ID                  | Серверный порядок и критерии сохранены; неверный ID отклонён, done открывается только доказанным дефектом           |
| A49 | Два цикла без изменения кода/прогресса/замечаний                      | waiting_input(no_progress), перефразирование не сбрасывает детектор                                                 |
| A50 | Inconclusive с допустимыми и запрещёнными missing_evidence            | Разрешённые данные собираются максимум за 2 возврата; новые argv/права не исполняются                               |
| A51 | Одна модель у реализации и verifier                                   | Подсказка необязательна, сессии разделены, pipeline разрешён                                                        |
| A52 | Много repair-продолжений, раздутый контекст                           | После лимита новая сессия с актуальным ограниченным evidence; полная история остаётся в хранилище                   |
| A53 | Новое сообщение/смена задачи во время Run                             | Snapshot не меняется; resolution-artifact не меняет критерии, новая задача создаёт новый Run                        |
| A54 | GC/backup во время работы и сбой очистки/restore                      | Активные/связанные evidence защищены, backup согласован, stale lease не вызывает auto-resume                        |
| A55 | Неподдержанный schema major/features и миграция                       | Нет молчаливого исполнения; новая версия шаблона, старый snapshot сохранён                                          |
| A56 | Preflight успешен, затем изменились файлы/доступ/резервация           | Worker повторяет изменяемые проверки под владением; старый preflight не разрешает действие                          |
| A57 | Null/missing/неверный тип в AST и результат прошлой итерации          | Unknown отделён от false, ошибки типов явны, stale evidence не выбираются                                           |
| A58 | Закрытие консоли, reboot, optional autostart/update                   | Launcher живёт независимо, после reboot recovering; автозапуск не устанавливается без выбора                        |
| A59 | 429/5xx/timeout после отправки LLM-запроса                            | Retry только при доказанном safe/idempotent; неизвестный исход не повторяется автоматически                         |
| A60 | Потеря lease/БД после внешнего эффекта, поздний ответ                 | Новых действий нет, stale generation не фиксирует переход, результат решает reconciliation                          |
| A61 | Создание/копирование agent/llm-групп heavy/flash, порядок и параметры | API/UI сохраняют настройки и ревизии; разные роли выбирают heavy/flash, конфликты редактирования отклоняются |
| A62 | Пустая/смешанная группа, дубликаты, неверные ссылки/параметры; несовместимый кандидат | Невалидная конфигурация не запускается; отключённые/несовместимые позиции пропускаются с причиной, следующий подходящий доступен |
| A63 | Первые кандидаты недоступны: агентная группа | Порядок соблюдён, первый успешный кандидат завершает выбор; межharness-переход создаёт новую сессию с контекстом и прежними правами |
| A64 | Первые кандидаты недоступны: LLM-группа из разных подключений | Следующий получает тот же логический запрос/evidence, собственные параметры и credentials; причины и фактическая модель сохранены |
| A65 | Все кандидаты недоступны; доступ восстановлен | waiting_input(model_group_exhausted) с причинами; нет бесконечного обхода; явное продолжение использует snapshot и прежние счётчики |
| A66 | Timeout/обрыв после отправки, частичные правки, неверный JSON/failed/permission | Unknown требует сверки, fallback не дублирует эффекты; ошибки формата, вердикт и разрешения не запускают перебор моделей |
| A67 | Retry, fallback, исчерпание бюджета, stop/cancel/pause между кандидатами | Вызовы учтены в одном StepExecution с отдельными попытками; общий лимит и команды управления запрещают дальнейший dispatch |
| A68 | Изменение/архивация группы во время Run, restart/resume/reconnect | Snapshot и позиция сохранены, активная попытка сверяется до следующей; история доступна без браузера, новый Run использует новую ревизию |
| A69 | Старый pipeline с direct; импорт/экспорт групп и переопределение в проекте | Прежнее поведение без fallback; нет секретов/скрытой перезаписи групп, ссылки разрешены, выбор переопределён целиком без слияния списков |
| A70 | Новый шаг/цикл после fallback и resume прерванного StepExecution | Новое посещение, включая repair, начинает с первого приоритета; resume сохраняет кандидата, при недоступности идёт дальше; бюджеты не сбрасываются |

Для внутреннего MVP обязательны A01–A22 (A04 — для первого harness), A24–A27, A29–A39, A41–A44 (импорт пока через API), A46–A53, A56–A57 и A59–A70 (A63 — внутри первого harness; импорт групп A69 пока через API). Retention/backup/load, поставка/update/autostart, межharness-переключение и полноценный импорт через конструктор доводятся к полной v1: все A01–A70, оба harness и конструктор обязательны. Результаты фактических проверок ведутся отдельно; таблица не утверждает, что тесты уже пройдены.

## 5. Стратегия проверок

Модульные тесты покрывают самостоятельную логику условий, актуальность контекста, счётчики, разрешение параметров и валидацию графа. Интеграционные тесты используют реальную временную SQLite и Git-репозитории. Сценарии восстановления выполняются с настоящими дочерними процессами и принудительными сбоями в точках до/после внешнего действия.

Для групп проверяются порядок кандидатов, параметры/переопределения, несовместимость, exhausted и отсутствие циклического fallback. Fake harness и управляемый HTTP-сервер воспроизводят подтверждённый отказ, 429/5xx до/после исполнения, неизвестный исход, восстановление между кандидатами и остановку на общем бюджете. Отдельный UI-сценарий создаёт agent/llm heavy/flash, меняет приоритеты, назначает роли и показывает реальное переключение/историю; интеграционная проверка доказывает, что credentials подключений не смешиваются.

Контрактные тесты адаптеров воспроизводят обезличенные события подтверждённых версий harness. Отдельные реальные smoke-тесты проверяют подключение, модель, простой шаг, прерывание и продолжение. Их результат фиксируется отдельно: fake-адаптер не доказывает работоспособность внешнего сервиса.

UI-проверки сосредоточены на трёх сквозных сценариях: создать проект/чат и запустить пресет; собрать/сохранить циклический pipeline; закрыть браузер, открыть снова и продолжить остановленный Run. Простое оформление не требует тестов, повторяющих реализацию компонентов.

Ключевой тест независимости: старт через API, отсутствие любых подключённых клиентов во время нескольких переходов, завершение через worker и последующее чтение сохранённого результата. Отсутствие браузера, перезапуск API, смерть worker и reboot — разные тесты.

Windows gate включает реальные Job Objects/потомки, 8.3/junction/subst/длинные пути, Git hooks/signing и DPAPI. Если CI не поддерживает определённый filesystem feature, пропуск фиксируется как unverified и закрывается отдельной проверкой на поддержанной среде. Негативные security-fixtures и сбои до/во время/после внешнего вызова не заменяются моками процесса.

Для нагрузки используется зафиксированный Windows/SSD-профиль: 100 000 событий, большие артефакты и slow SSE. Цели p95: snapshot < 500 мс, первая страница истории < 1 с, доставка сохранённого события при активном клиенте < 1 с; буферы не растут с длительностью запуска. Метрики будущие, их нужно измерить и приложить к приёмке.

## 6. Контрольные результаты

| Результат               | Этапы                  | Что можно показать                                                               |
| ----------------------- | ---------------------- | -------------------------------------------------------------------------------- |
| Серверный прототип      | Контракты 0, затем 1–5 с 2A | Граф/fake, группы и fallback, очередь/SSE, управление/recovery, диагностический CLI |
| Серверный пресет        | 7–8, без ожидания 6    | Fake + Command/LLM/Git, весь цикл с PlanItem, evidence и коммитами               |
| Внутренний MVP          | 6A и 9                 | Первый реальный harness, LLM, группы heavy/flash с fallback, пресет и базовый UI |
| Функционально полная v1 | 6B, 10–11              | Второй harness, оба каталога, fallback между harness, полный конструктор и наблюдение |
| Готовый выпуск v1       | 12                     | Все A01–A70, нагрузка, безопасность, backup/update и инструкция чистой установки |

Этапы разбиваются на небольшие изменения с собственным критерием готовности. Календарные сроки оцениваются после проверки интеграций на этапе 0. Реализация начинается с этого этапа; создание настоящих документов не означает выполнение перечисленных задач.

## 7. Покрытие архитектурных замечаний ревью

Рекомендованные отдельные темы объединены в четыре контракта, чтобы одно правило не дублировалось в четырнадцати файлах. Таблица связывает решение, реализацию и проверку.

| Тема ревью                             | Где принято решение                              | Этапы / приёмка                 |
| -------------------------------------- | ------------------------------------------------ | ------------------------------- |
| Машины состояний и команды             | STATE_MACHINES                                   | 4–5; A11–A13, A24–A26, A60      |
| Очередь, SQLite, события и ошибки      | RUNTIME_CONTRACTS §§1–4                          | 1, 4–5; A27–A28, A60            |
| SSE и локальная авторизация            | SECURITY_AND_OPERATIONS §3, RUNTIME_CONTRACTS §2 | 1, 4, 9; A15, A28, A43          |
| Процессы, порты и capability           | RUNTIME_CONTRACTS §§5–6                          | 0, 5, 6A/6B; A29–A30, A46–A47   |
| Command, CollectContext, AST и schema  | EXECUTION_CONTRACTS §§1–4                        | 3, 7, 10; A33–A34, A50, A55–A57 |
| Git, dirty tree, hooks и baseline      | EXECUTION_CONTRACTS §§6–7                        | 8; A19–A20, A35–A37             |
| Threat model и доверие импорту         | SECURITY_AND_OPERATIONS §§1–2                    | 0, 3, 10, 12; A41–A44           |
| Секреты и provider URLs                | SECURITY_AND_OPERATIONS §4                       | 1–2, 7, 12; A05, A44–A45        |
| Windows-пути                           | SECURITY_AND_OPERATIONS §5                       | 0–2, 5, 12; A18, A31–A32        |
| Бюджеты, retry и retention             | RUNTIME_CONTRACTS §7, SECURITY_AND_OPERATIONS §6 | 4, 7, 12; A38–A40, A54, A59     |
| PlanItem, evidence и контекст          | EXECUTION_CONTRACTS §§4–5                        | 2–3, 7–8; A48–A52               |
| Неизменяемые входы и изменения лимитов | STATE_MACHINES §5                                | 2, 5, 9; A38, A53               |
| Дистрибуция, backup, диагностика       | SECURITY_AND_OPERATIONS §§6–8                    | 0–1, 5, 12; A23, A45, A54, A58  |
| Декомпозиция релиза и ранний UI/CLI    | Этот план §§1, 3, 6                              | 5, 6A, 9; gate MVP перед 6B/10  |
| Группы моделей, приоритеты и fallback | PROJECT_DESCRIPTION §7.5; синхронизация четырёх контрактов в этапе 2A | 2A–7, 9–11; A47, A61–A70 |
